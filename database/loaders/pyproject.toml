[project]
name = "pharos-mock-data-loaders"
version = "0.1.0"
description = "JSON-to-PostgreSQL loaders for Pharos dashboard mock data"
requires-python = ">=3.12"
dependencies = [
    "psycopg[binary]>=3.2,<4",
]

[tool.uv]
package = false

